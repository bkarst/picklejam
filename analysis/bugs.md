# PickleLoko — Bug Inventory

> **Date:** 2026-07-02 · **Basis:** a fresh, six-domain code audit (events/payments data
> layer, community/directory data layer + streams, API route handlers, pure algorithms,
> client React, and SSR/SEO/rendering). Every finding below was traced to specific lines and,
> where noted, confirmed against real call paths. Evidence cited as `file:line`.
>
> **Scope:** this is a companion to `analysis/technical-improvements.md`. The six issues that
> doc already covers are **not repeated here** (tournament-wizard `cityKey`; waitlist deferred
> capture never captured / webhook not checking `payment_status` / no `payment_intent.canceled`
> handler; `reconcileGroupMemberCount` pending-vs-active mismatch; `checkinsTodayCount`/CITYDAY
> reset + `playerCount` double-count; no prod Streams Lambda / reconcile has no callers; DUPR
> connect stub). The bugs here are *new* — concrete runtime defects, ordered by severity.
>
> **Headline:** the money spine has several correctness holes that only surface against **real
> Stripe** (the FakeGateway masks them in every test) — most seriously, a Checkout change in the
> pinned API version means the app stores an empty `paymentIntentId` and cannot refund anything
> in production. Beyond payments there is one **stored-XSS** sink; a **compliance** data-loss on
> profile edits (the unsubscribe list is wiped); an **authz** hole letting anyone post events
> into a private group; a **display** bug that renders every outing time in UTC in production
> (no outing ever stores a timezone); and a round-robin **"pools + bracket"** event that a single
> early tap (or one tied bracket score) permanently bricks. 14 HIGH, 29 MEDIUM, 23 LOW in all.
>
> ---
>
> ### ⏳ Fix progress (updated 2026-07-03)
>
> **Fixed & verified: ALL 14 HIGH (H1–H14) + ALL 29 MEDIUM (M1–M29) + ALL 23 LOW (L1–L23).** Each was re-verified against the
> code before fixing; fixes carry regression tests where practical (many proven red→green), and
> most were executed end-to-end against a local DynamoDB (dynalite) — not just code-traced.
> Per-item fix notes are inline below (`**✅ FIXED**`). A few report claims were corrected in
> passing (noted per item): H1's suggested `expand:["payment_intent"]` won't work under deferred
> PI creation (backfill from the webhook is the real fix) and ladders is **not** refund-bricked;
> H2's `= pending` guard would break doubles (used `IN (pending, partnerPending)`); H8's own
> integration test encoded the vulnerable design and was rewritten. **ALL 66 findings fixed & verified — 14 HIGH + 29 MEDIUM + 23 LOW.** The entire inventory is closed; the full unit/component/integration suite is green (no remaining reds).

---

## HIGH — fix before real traffic / real Stripe

### H1. Every real-Stripe refund is bricked: empty `paymentIntentId` stored at registration
- **Confidence:** CONFIRMED (code path; trigger is documented Stripe behavior on the pinned API version)
- **Where:** `lib/stripe/gateway.ts:124-125` (`pi?.id ?? ""`), stored at `lib/data/tournaments.ts:630`; consumed at `:737`, `:883`; same in `lib/data/leagues.ts:668,737,1090` and `lib/data/ladders.ts`.
- **Bug:** The app pins Stripe API version `2026-06-24.dahlia` (`lib/stripe/index.ts:42`). Since `2025-03-31.basil`, payment-mode Checkout Sessions create the PaymentIntent **lazily** — `session.payment_intent` is `null` at creation and `createCheckoutSession` doesn't `expand` it — so the gateway stores `paymentIntentId: ""` on every real registration. Downstream, `reg.paymentIntentId ?? input.paymentIntentId` never falls through to the webhook's real PI because `??` does not coalesce `""`, so the Payment receipt is written with `paymentIntentId: ""` and the REG is never backfilled.
- **Failure:** In any real-Stripe deployment, `refundRegistration`/`refundLeagueRegistration` hit `if (!reg.paymentIntentId) badRequest("no captured payment to refund")` for **every** paid registration; the organizer refund route 400s, and `cancelTournament` throws on the first paid reg *after* META is already `cancelled`, aborting the mass refund. Works fine under FakeGateway (`pi_fake_n`), so no test sees it. **Fix:** `expand: ["payment_intent"]` on session create, or backfill `paymentIntentId` from the webhook (and use `||`, not `??`).
- **✅ FIXED:** `confirmRegistrationPayment`/`confirmLeaguePayment` now resolve `reg.paymentIntentId || input.paymentIntentId` and **backfill** it onto the REG during the atomic paid-flip — fixing the organizer refund guard, the receipt lookup, and the `charge.refunded` reconciliation at once. **Correction:** `expand:["payment_intent"]` would NOT help (deferred PI doesn't exist at session-create); the webhook backfill is the only reliable fix. **Correction:** ladders is NOT refund-bricked (no organizer-refund path; its receipt already stores the webhook's real PI).

### H2. Refunded / cancelled registrations resurrect to `paid` (webhook condition is `<> paid`, not `= pending`)
- **Confidence:** CONFIRMED
- **Where:** `lib/data/tournaments.ts:717` (`condition: "paymentStatus <> :paid"`), `lib/data/leagues.ts:654-659`. (Ladders got it right: `condition: "paymentStatus = :pending"`, `ladders.ts:565`.)
- **Bug:** The atomic paid-flip only guards `paymentStatus <> :paid`. Any non-paid state — `cancelled`, `refunded`, `partiallyRefunded` — satisfies it and is flipped back to `paid`, a second Payment receipt is written, and analytics re-fire.
- **Failure:** (a) Out-of-order siblings — `checkout.session.completed` marks paid; organizer refunds; the delayed `payment_intent.succeeded` (different event id, passes dedupe) flips `refunded → paid`, writes a duplicate receipt, and the reg is "paid" holding no capacity spot → `registeredCount` permanently undercounts → later oversell. (b) Organizer refunds a *pending* reg → `cancelled` + spot released; the registrant's still-open Checkout tab completes → reg resurrects to `paid` with no spot claimed. League variant is worse: the REG key is per-uid (`REG#<uid>`), so completing a stale div-A session flips the user's *current* div-B registration to paid at div-A's price.
- **✅ FIXED:** flip condition changed to `paymentStatus IN (:pending, :partnerPending)` in tournaments + leagues (only a non-terminal awaiting-payment state can flip to paid). **Correction:** the report's `= :pending` would break doubles (a `partnerPending` reg is created with a live Checkout and must still confirm), hence the two-value `IN`. Also closed the "league variant is worse" case with a `reg.did !== did` guard in `confirmLeaguePayment` (rejects a stale cross-division session). Regression covered by the tournaments/leagues webhook integration tests.

### H3. Users can pay into a cancelled tournament/league; money is captured and never refunded
- **Confidence:** CONFIRMED
- **Where:** `lib/data/tournaments.ts:696-767` (`confirmRegistrationPayment` never checks event status) vs `:938-960` (`cancelTournament` refunds only `PAID_STATES`, skips `pending`, never expires Checkout sessions); same in `lib/data/leagues.ts`.
- **Bug:** Cancel flips META to `cancelled` and refunds already-paid regs, but pending regs' Checkout sessions are left live and payment confirmation ignores event status.
- **Failure:** Organizer cancels while a registrant is mid-checkout; the registrant pays minutes later → webhook flips the reg `paid`, funds land in the organizer's connected account, and the mass-refund loop already ran — so it is never refunded. Customer paid for a cancelled event with no automated recovery.
- **✅ FIXED:** `confirmRegistrationPayment`/`confirmLeaguePayment` now fetch event status and, if `cancelled`, auto-refund the just-captured payment (organizer-initiated → app fee refunded) and suppress the confirmation analytics. Retry-safe (also checked on the already-paid short-circuit; refund helpers are no-ops on an already-refunded reg) and best-effort (catch-and-log so a failed auto-refund can't wedge Stripe retries). Left session-expiry-on-cancel as a recommended UX enhancement (not airtight vs. the race; the confirm-side auto-refund is).

### H4. Concurrent partial refunds double-refund at the gateway and lose the ledger update
- **Confidence:** CONFIRMED
- **Where:** `lib/data/payments.ts:154-196` — read at `:155`, gateway call at `:174`, **unconditional** `SET refundedAmount = :ra` at `:184-189`; no `ConditionExpression` on prior `refundedAmount`, no Stripe idempotency key on `refunds.create`.
- **Bug:** Read-compute-refund-write with the over-refund check (`newRefunded > total`) evaluated against a stale read.
- **Failure:** Organizer double-clicks a 50% partial refund. Both requests read `refundedAmount = 0`, both pass the excess check, both call `createRefund` — Stripe accepts both (sum ≤ charge) → customer gets 100% back while 50% was intended. Both then `SET refundedAmount = 50%` (last-write-wins) → ledger says half-refunded; a later "remaining 50%" refund is attempted against an already-fully-refunded charge.
- **✅ FIXED:** `refundPayment` reworked to **reserve-first optimistic concurrency** — a conditional `SET refundedAmount` guarded on the prior value commits the ledger slot BEFORE the gateway call, so a concurrent rival fails the condition and is rejected before touching Stripe (409-style "refund conflict"); gateway failure rolls the reservation back. Added an **idempotency key** to the gateway `createRefund` (`RefundInput.idempotencyKey`, honored by both Real + Fake gateways). **Note:** gateway-first-then-write can't be made correct (commits before detecting the conflict); reserve-first is the only sound ordering. Residual: lost-response HTTP retry needs a caller-supplied token (out of scope).

### H5. JSON-LD stored XSS — user content injected into `<script>` without escaping
- **Confidence:** CONFIRMED
- **Where:** `components/JsonLd.tsx:15` (`dangerouslySetInnerHTML={{ __html: JSON.stringify(data) }}` — no `<` → `<` escaping); user content reaches it via `reviewJsonLd` (`lib/seo/jsonld.ts:212-213`, review `title`/`body`), outing title/description (`sportsEventJsonLd`), group name/description, player `displayName`, and round-robin event/entrant names (the RR create flow needs **no login**). The repo's own Next docs mandate the escape (`node_modules/next/dist/docs/01-app/02-guides/json-ld.md`).
- **Bug:** The HTML parser closes a `<script>` at the first literal `</script>` regardless of JSON string context, so a crafted string breaks out of the JSON-LD block.
- **Failure:** Anyone posts a court review with body `</script><script>…exfiltrate cookies…</script>`. The court page is ISR (`revalidate = 3600`) and bakes it into cached HTML, serving the attacker's script to every visitor for up to an hour per regeneration — persistent, unauthenticated-victim XSS. Same via an anonymous round-robin event title. **Fix:** escape `<`, `>`, `&`, ` / ` in the serialized string.
- **✅ FIXED:** `JsonLd.tsx` now serializes via a `serializeJsonLd` helper that escapes `<`, `>`, `&` and the JS line/paragraph separators (U+2028/U+2029) to their `\uXXXX` forms — the `serialize-javascript` set, strictly stronger than the Next docs minimum; output stays valid JSON. Verified at runtime in Node that a `</script>` breakout payload is neutralized and still round-trips. Corrected the false "no user HTML" comment.

### H6. Editing a profile wipes notification prefs **and the unsubscribe suppression list** (CAN-SPAM/RFC 8058)
- **Confidence:** CONFIRMED
- **Where:** `app/api/account/profile/route.ts:44-101` builds `next: ProfileInput` (which has **no** `notifPrefs`/`unsubscribed`/`checkinVisibility` fields), then `buildProfileItem` (`lib/data/users.ts:188-210`) + `putProfileWithUsername` (`:138`) do a **full `Put` replace**. But `unsubscribed`/`notifPrefs` live on the same PROFILE item and are written via `updateItem` (`lib/data/notifications.ts:170-195`).
- **Bug:** Every profile edit rebuilds the item without those attributes, silently erasing them.
- **Failure:** A user one-click unsubscribes (suppression list set), later edits their display name → full `Put` drops `unsubscribed` → `resolveEmailAllowed` (`lib/notify.ts:60`) no longer suppresses → emails resume to an unsubscribed address (compliance failure). Same wipe for muted channels / quiet hours.
- **✅ FIXED:** added `notifPrefs`/`unsubscribed`/`checkinVisibility` to `ProfileInput`, emitted them in `buildProfileItem`, and had the profile PUT carry them from `current` — so the full-Put edit is lossless. Verified the profile PUT is the only full-Put path (`completeOnboarding` uses a partial update). Regression test added (`profile.test.ts`): set unsubscribe + prefs, edit display name, assert both survive.

### H7. Write-IDOR: any authenticated user can post "meet-ups" into any group, including private ones
- **Confidence:** CONFIRMED (reported independently by the API and community audits)
- **Where:** `app/api/outings/route.ts:66-68` (only checks `groupId` is a string) → `lib/data/outings.ts:204-216` (`createOuting` writes a `MEETUP#` row into `GROUP#<groupId>` with no membership guard). Every other group mutation funnels through `requireManager`/`getGroupMember`; this one bypasses them.
- **Bug:** No check that the caller is a member (let alone manager) of the group, or that the group exists.
- **Failure:** An attacker (or kicked ex-member) POSTs `/api/outings` with `hostType:"GROUP"`, `groupId:<victim private group>`, any valid court. `getGroup` (`lib/data/groups.ts:319-356`) then hydrates the injected meet-up into the group's members-only schedule. Unauthorized write/vandalism of a private resource.
- **✅ FIXED:** the outings POST now requires an ACTIVE `getGroupMember(groupId, uid)` for `hostType:"GROUP"`, else 403 — blocking non-members, ex-members, pending/invited requesters, and non-existent groups (no membership row). Enforced at the route (its single caller); set the boundary at active-membership (the correct security boundary for a members-only resource) rather than manager-only.

### H8. Division skill/DUPR eligibility gate is client-forgeable
- **Confidence:** CONFIRMED
- **Where:** `app/api/tournaments/[id]/register/route.ts:36-37` and `.../leagues/[id]/register/route.ts:35-36` forward `body.dupr`/`body.skill`; `resolveDupr` returns the override without reading the stored `RATING#` row (`lib/data/tournaments.ts:537-540`), and `skill` has no stored fallback at all.
- **Bug:** The rating gate trusts a request-body value over the stored rating.
- **Failure:** Division requires `duprMin: 4.0`; a 3.0 player POSTs `{ did, dupr: 4.5 }` → `assertRatingGate` passes → registered and charged for a division they're ineligible for (fairness + refund burden).
- **✅ FIXED:** the gate is now resolved SERVER-SIDE from stored ratings only — `resolveDupr(uid)` reads `RATING#DUPR` and requires `verified:true` (self-entered unverified DUPR no longer counts); added `resolveSkill(uid)` reading the self-reported `RATING#SELF`. Removed `dupr`/`skill` from `RegisterOptions`/`LeagueRegisterOptions` and the route body forwarding. **Note:** the report's own integration test encoded the vulnerable design (asserted the override drives the gate) — rewritten to the secure spec (no-stored/out-of-range/unverified all 403). The underlying DUPR-connect stub (verified DUPR is itself client-supplied) is the separately-tracked issue; this makes the gate structurally correct for when real OAuth lands.

### H9. Ladder rejoin after a refund creates a duplicate rung; the next payment is silently dropped
- **Confidence:** CONFIRMED
- **Where:** `lib/data/ladders.ts:474-496` (terminal rungs pass the `ACTIVE_RUNG` guard; `appendRung` adds a second rung for the same uid), `:555`/`:633` (`getRungs(...).find(r => r.uid === uid)` returns the lowest-position match), `:562-575`.
- **Bug:** A refunded rung stays on the board; rejoining appends a second rung, and `confirmLadderPayment` locates the rung by position order → the OLD refunded rung, whose conditional flip fails → the catch returns `alreadyPaid: true`.
- **Failure:** Player joins → refunded (rung 5 `refunded`) → rejoins and pays → webhook confirms against rung 5 → condition fails → treated as a duplicate delivery, 200. Money captured; the new rung stays `pending` forever; no receipt. A later `reorderBoard` collapses the duplicate uid in a `Map`, writing one rung to two positions and corrupting the board.
- **✅ FIXED:** `registerForLadder` now REUSES a terminal rung's slot on rejoin (clean pending entry via full-replace `putItem`, dropping stale `refundedAmount`/`paymentIntentId`) instead of appending a duplicate — maintaining one-rung-per-uid. Also hardened `confirmLadderPayment` to prefer the PENDING rung (defense against legacy duplicates). Regression test added (`ladders.test.ts`): join → refund → rejoin → pay asserts one paid rung in the reused slot + two receipts. Executed green against dynalite.

### H10. Group members are shown "Join group" (prop captured into state once, never resynced)
- **Confidence:** CONFIRMED
- **Where:** `components/groups/MembershipButton.tsx:47` (`useState(membership)`), fed by `app/groups/[id]/GroupDetailClient.tsx:31-40`, which renders the button *while `useGroup` is still loading* (`membership={data?.membership ?? null}`). No key, no sync effect.
- **Bug:** The button copies the prop into `committed` at mount and never updates when the query resolves.
- **Failure:** A signed-in active member (or pending requester) opens `/groups/[id]`; the button mounts with `membership=null`, the query resolves to `{role:"member",status:"active"}`, but the button still says "Join group" / "Request to join". Clicking re-POSTs `/join` for an existing member. Owners see a join button next to their own "Manage group" link.
- **✅ FIXED:** `GroupDetailClient` gives `MembershipButton` a membership-derived `key` (`${role}:${status}` / `"none"`) so it remounts and re-seeds `committed` when the overlay resolves; added a `loading` prop that renders a non-clickable placeholder while membership is unknown (closes the "clicking re-POSTs join" window). Chose keyed-remount over a sync effect (an effect races the post-mutation refetch). Regression tests added to `MembershipButton.test.tsx`.

### H11. Any non-numeric keystroke in a fee field crashes the league create wizard (all state lost)
- **Confidence:** CONFIRMED
- **Where:** `components/leagues/CreateLeagueWizard.tsx:107-110` (`previewFace` `useMemo` calls `moneyFromMajor(fee || "50")` on the raw text input every render); `moneyFromMajor` throws (`lib/money.ts:41`) for anything `Number()` can't parse (`"$80"`, `"80,50"`, `"-"`) or negatives. The fee inputs are `type="text"`. `CreateTournamentWizard` guards this exact case (`:108-116`); the league wizard doesn't.
- **Failure:** Organizer on step 2 pastes "$80" (or types a stray char) → render throws → Next error boundary replaces the page → **all wizard state (title, city, divisions) lost**.
- **✅ FIXED:** added an `isValidFeeInput` guard (non-empty, finite, non-negative); `previewFace` falls back to the placeholder unless valid, and `canAdvance` requires a valid numeric fee (matching `CreateTournamentWizard`). Regression test added (`CreateLeagueWizard.test.tsx`), **proven red→green** — reverting fails with the exact `invalid major amount: $` throw.

### H12. A dismissed auth intent fires on the next, unrelated sign-in (can auto-launch Stripe checkout)
- **Confidence:** CONFIRMED
- **Where:** `components/auth/AuthProvider.tsx:187-197` stores `pendingIntent.current`; `openAuth` (`:182-185`, called from Header/AccountMenu "Sign in") never clears it; the resume effect (`:200-208`) runs the stashed intent on any sign-in while the modal is open.
- **Bug:** Closing the modal without signing in never clears the pending intent.
- **Failure:** Signed-out user clicks "Continue to payment" on a tournament (intent = register + redirect to Stripe), dismisses the modal, later signs in from the header to check their profile — and is instead immediately registered and bounced to Stripe Checkout for the abandoned event.
- **✅ FIXED:** `openAuth` now clears `pendingIntent` (a bare sign-in carries none; `requireAuth` reordered to set its intent after), and a `handleModalOpenChange` clears it on dismiss (safe: `AuthModal` only reports a close on explicit dismiss — success is closed by the resume effect). Regression tests added to `AuthModal.test.tsx` (positive control + the dismissed-intent case), **proven red→green**.

### H13. Every outing time displays in the server timezone (UTC in prod) — no outing ever stores a `tz`
- **Confidence:** CONFIRMED
- **Where:** `components/outings/OutingWizard.tsx:455-470` omits `tz` from the create payload; the only writer of `tz` is `app/api/outings/route.ts:100` reading `body.tz`, which **no client sends**. Fallbacks then use the runtime TZ: `components/outings/format.ts:10-30` (`toLocale*String` with no `timeZone`) and `app/outings/[id]/page.tsx:167-171` (forecast-day match).
- **Bug:** The model, formatters, and weather-day matcher are all "timezone-aware via optional `outing.tz`", but nothing ever populates it, so every branch falls back to the server's timezone. The outing detail page and `OutingCard` are server-rendered.
- **Failure:** An organizer in Kansas City creates a 6:00 PM game (stored correctly as `…T23:00:00Z`). In prod (UTC server) the outing page renders "11:00 PM" for every viewer; a 7:00 PM PDT game shows the *next day's* date. The weather chip computes `dayStr` in UTC (`2026-07-05`) while Open-Meteo returns court-local dates, so it shows the wrong day's forecast. (Same root cause as **M2**/**L15** on the directory side.)
- **✅ FIXED:** both outing-create paths (`OutingWizard`, group meet-up `ManageGroupClient`) now populate `tz` from a new `browserTimeZone()` helper — consistent with the browser-local `startTs`, so viewers read the intended local time instead of the server's UTC. The route/data-layer/formatter plumbing already supported `tz`; only population was missing. Regression tests: formatter unit test (locks "6:00 PM" Central vs the buggy "11:00 PM" UTC) + `tz` round-trip in the outings integration test. **Note:** ideal long-term is an IANA tz on the court (from lat/lng at ingest) — flagged in the helper.

### H14. Round-robin "pools + bracket" bricks on one early tap, and a tied bracket score deadlocks the event
- **Confidence:** CONFIRMED (executed repro)
- **Where:** `lib/roundrobin/engine/pools.ts:244-255` (`poolsNext`) and `:184-194` (`nextBracketRound` returns `null` for "in progress"); `lib/data/roundrobin.ts:526-537` (`advanceRound` treats *any* `null` as "event over" → `status="complete"`, `championId=null`); `RunConsole.tsx:109-110` (`canAdvance` never checks the round is fully scored); tie acceptance at `roundrobin.ts:435-443`.
- **Bug:** `nextRound()` returns `null` in three different states — pools not fully scored, bracket round in progress, and bracket finished — which `advanceRound` cannot tell apart. Separately, `recordScore` accepts tied scores, and a tied bracket match makes `decidedWinner()` null forever.
- **Failure (both executed):** (1) 8 entrants, 2 pools: enter a semifinal as `11–11` → no Final is ever generated, `isComplete: true`, `champion: null`. (2) On the last pool round the "Next" button is enabled even with unscored matches → one tap → `advanceRound` writes `status="complete"`, `championId=null`, `editable` goes false → the organizer can no longer enter scores. The event is bricked one round in.
- **✅ FIXED:** (A) `advanceRound` only finalizes when `isComplete(config, completed)` is true; otherwise it rejects ("score every match first") instead of completing with a null champion. (B) `recordScore` rejects a tie on a poolsBracket BRACKET match (`round > poolRoundCount`) — pool matches can still tie (standings award draws). (C) `RunConsole.canAdvance` also requires the current round fully scored. Re-exported `poolRoundCount`. Regression tests added to `roundrobin.test.ts` (unscored-advance rejected; pool ties allowed / bracket ties rejected / decisive play crowns a champion), **proven red→green** against dynalite.

---

## MEDIUM

### M1. No `LastEvaluatedKey` pagination anywhere — mass refunds, dashboards, and rosters truncate at 1 MB
- **Confidence:** CONFIRMED · `lib/db/client.ts:108-161` (`query` returns one page; `lastKey` exposed but no data-layer caller loops), e.g. `cancelTournament` (`tournaments.ts:938-960`), `getMyPayments` (`payments.ts:116-123`), ladder reads (`ladders.ts:315-321,763-769`).
- A tournament whose registration partition exceeds ~1 MB (thousands of regs): `cancelTournament` mass-refunds only the first page — the rest keep their money for a cancelled event, no error surfaced. Same truncation undercounts organizer-dashboard revenue and seeds brackets/schedules from partial rosters. *(Money-loss for large events; MEDIUM only because it needs a very large partition.)*
- **✅ FIXED:** added a `queryAll<T>` helper (follows `LastEvaluatedKey` to exhaustion) and applied it to the correctness-critical full-partition reads: `getMyPayments`, `getRungs`, `getTournament`, `getLeague`, and the three RR partition reads (`getRrEvent`/`recordScore`/`advanceRound`). Deliberately NOT blanket-applied — user-facing lists keep cursor pagination. Regression test added (`data-layer.test.ts`): `query` with a small `limit` truncates + returns a cursor; `queryAll` returns everything.

### M2. `/play` city games default to the wrong day (server-UTC vs court-local key)
- **Confidence:** CONFIRMED · `app/play/[country]/[state]/[city]/page.tsx:24-36` (`todayYyyymmdd` uses `new Date().get*` = server-local) vs the court-local `yyyymmdd` key in `lib/data/outings.ts:128`. The courts city page does this right via `courtLocalDay` (`app/courts/.../page.tsx:59`); only `/play` is wrong.
- On a US west-coast city, from late afternoon until midnight UTC has already rolled to the next day, so the default view shows *tomorrow's* games and hides tonight's 7pm game — during peak evening hours.
- **✅ FIXED:** the default day now uses `courtLocalDay({ lng: cityItem.centroidLng ?? -98 }, nowMs())` (the same tested helper the `/courts` city page uses), threaded through `resolveDay(raw, fallbackDay)`; `?date=` still overrides. Verified in Node: 8pm PDT → tonight (`20260704`) not tomorrow (`20260705`). Reuses the directly-unit-tested `courtLocalDay`.

### M3. Court reviews: `limit` applied before sort — the newest reviews can be permanently invisible
- **Confidence:** CONFIRMED · `lib/data/reviews.ts:38-50` passes `limit` into the DynamoDB Query (rows keyed `REVIEW#<uid>`, uid-lexicographic), then sorts the returned page by `createdAt`. Sort-after-limit.
- A court with 25 reviews requested at `limit:20` returns the 20 lowest-uid reviews, then sorts *those*; the actual 5 newest (high-sorting uids) never appear regardless of pagination. Same defect for `sort:"helpful"`.
- **✅ FIXED:** `getCourtReviews` now reads the FULL review set (`queryAll`), sorts, THEN slices to `limit` (reviews-per-court are bounded). Dropped the meaningless SK cursor from the signature (the only caller never used it). Regression test added (`community.test.ts`): a newest review with a high-sorting uid is returned at `limit:1` for both `recent` and `helpful`, **proven red→green**.

### M4. RSVP downgrade (going → declined/maybe) frees a spot but never promotes the waitlist
- **Confidence:** CONFIRMED · `lib/data/outings.ts:389-392` decrements `goingCount`, but promotion lives only in `cancelRsvp` (`:483-510`), not the POST path.
- Capacity 4, full, 2 waitlisted. An attendee posts `declined` (the natural "can't make it") → `goingCount=3`, waitlist untouched → waitlisters stay stuck while the free spot goes to the next brand-new RSVP, jumping the queue.
- **✅ FIXED:** extracted a shared `promoteWaitlistIntoFreedSpot` helper (used by both `cancelRsvp` and the `rsvp` downgrade path) and called it whenever an existing `going` RSVP transitions to a non-going status — placed before the RSVP re-write so a going→waitlist move can't promote itself. Regression test added (`outings.test.ts`): a going→declined downgrade promotes the waitlist, **proven red→green**.

### M5. `cancelRsvp` waitlist promotion can oversell capacity (unconditional increment)
- **Confidence:** CONFIRMED · `lib/data/outings.ts:484-501` — decrements then promotes the waitlist head with an **unconditional** `ADD goingCount :1` (unlike `claimGoingSpot`, no `goingCount < capacity` guard).
- Capacity 4, full, waitlist non-empty: A cancels (4→3); concurrently E claims a spot (3→4, conditional passes); A's promotion then `ADD +1` → 5 > capacity. The "never oversold" invariant is violated.
- **✅ FIXED:** the shared promotion helper now claims the freed spot CONDITIONALLY via `claimGoingSpot(capacity)` before writing the promoted RSVP — if a concurrent claimer already refilled capacity, the claim fails and the waitlister is left in place (no oversell). Regression test added (`outings.test.ts`): a deterministic reconstruction of the race's critical moment (count at capacity when promotion runs), **proven red→green** (buggy code oversells to 2). *(Note: `Promise.all` didn't reproduce this race under dynalite's deterministic interleaving — hence the deterministic reconstruction.)*

### M6. RSVP / cancel counters aren't serialized per user — double-submits drift the counts
- **Confidence:** CONFIRMED · `lib/data/outings.ts:386-431`, `:474-481` — read-`existing` → adjust counter → blind `putItem`/`deleteItem`, no conditional/version guard.
- Double-click "Going": both see `existing=undefined`, both `claimGoingSpot` → `goingCount +2` for one attendee (burning a stranger's spot). Double-click cancel: both `ADD -1` → count under-reads and two waitlisters get promoted for one seat. A `attribute_exists`/conditional put closes it.
- **✅ FIXED:** `rsvp` rewritten as a **conditional-put gate** (optimistic concurrency on the exact prior status): apply counter deltas, commit the row via `putConditional` on `attribute_not_exists(pk)`/`#status = :sOld`, and on a lost race undo the deltas + return the winner's state; the freed-seat promotion is deferred to after a winning commit so a loser never promotes. `cancelRsvp` uses a conditional delete (`attribute_exists(pk)`) so only the winner decrements + promotes. Regression tests added (`outings.test.ts`): double-"going" → count 1 (not 2), double-cancel → count 0 (never −1), **both proven red→green**.

### M7. Waitlist positions duplicate after a status change off the waitlist
- **Confidence:** CONFIRMED · `lib/data/outings.ts:389-415` — `waitlistPos` is the post-ADD counter value; a waitlist→declined/going change decrements the count but doesn't renumber remaining positions (only `cancelRsvp` renumbers).
- Waitlist [A:1,B:2,C:3]; A declines → count=2 but C still pos 3; new joiner D gets pos 3 → duplicate with C, so `promoteFromWaitlist` (sorts by pos) orders C vs D arbitrarily.
- **✅ FIXED:** confirmed exactly as reported (`waitlistPos` is the post-ADD `waitlistCount`, so any departure that decrements the count without renumbering survivors lets the next joiner reuse an in-use position). Extracted the renumber logic from `cancelRsvp` into a shared `renumberWaitlistAfterDeparture(outingId, startTs, departedPos)` helper and now call it from the `rsvp` waitlist-departure path too (`sOld === "waitlist"`, deferred to *after* the conditional commit so a race-loser never renumbers). This keeps positions contiguous `1..waitlistCount`, so the next join at `count+1` is always unique. `cancelRsvp` now delegates to the same helper (DRY). Regression test `test/integration/outings.test.ts` ("M7: a waitlist → declined transition renumbers survivors…") proven red→green against dynalite: pre-fix the survivor stayed at its old position and the new joiner collided; post-fix all positions are distinct `{1,2,3}`.

### M8. Division spot double-release under concurrent refund/cancel (no conditional on the state transition)
- **Confidence:** CONFIRMED · `lib/data/tournaments.ts:867-881` (cancel branch sets `cancelled` unconditionally then releases from the stale read), `:908` vs `:827` (API refund vs webhook `charge.refunded` both release); same in `leagues.ts:1076-1087`.
- Two concurrent organizer refunds for one pending reg both read `pending`, both release → `registeredCount` drops 2 for 1 claim → later oversell. Or `refundRegistration` releases at `:908` while the `charge.refunded` webhook already released at `:827`.
- **✅ FIXED:** confirmed exactly as reported (report line numbers were stale after the earlier fixes, but all three release sites — the pending-cancel branch, the organizer API full-refund, and the `charge.refunded` webhook — released off a non-atomic read of `paymentStatus`; `releaseDivisionSpot` only guards against going *negative*, not against double-decrement when count > 1). Added a `transitionRegStatus(key, from, to)` helper (in both `tournaments.ts` and `leagues.ts`) that flips `paymentStatus` *conditionally* on `paymentStatus = :from` and reports whether THIS caller won; every release now fires only on the winning transition, so exactly one caller frees the spot. The webhook additionally skips a reg already `refunded` (an idempotent duplicate) so a `refunded→refunded` self-transition can't release twice; the API full-refund path can never read `refunded` (guarded by the earlier `PAID_STATES` check); the pending-cancel path is further guarded by `ACTIVE_REG`. Partial-refund row writes stay unconditional (they never release). Regression tests added to `tournaments.test.ts` + `leagues.test.ts` (two concurrent full refunds free the spot exactly once); the **tournaments** variant reproduced the pre-fix double-release **red→green** against dynalite (`registeredCount` 0 → 1). The concurrent race is timing-dependent under dynalite (the league pair serialized and didn't reproduce this run — same as the M5/M6 finding); the league test is an identical-code-path post-fix guarantee. Full tournaments+leagues suites green (27); typecheck clean.

### M9. Bracket re-seed orphans stale `BRACKET#` rows; `advanceBracket` routes off the ghosts
- **Confidence:** CONFIRMED · `lib/data/tournaments.ts:1093-1136` (writes only the new plan's keys, deletes nothing), `:1190-1198` (`finalRound = max(round)` over *all* rows).
- Seed with 8 paid (rounds 1–3 + 3rd-place); two refunds → re-seed writes 4-player rounds but the old R1M2/R1M3/R2M1/R3 rows remain → `advanceBracket` computes `finalRound=3` from ghosts, treats the real final as a semifinal, and shows phantom matches.
- **✅ FIXED:** confirmed (`seedBracket` was put-only — `Promise.all(items.map(putItem))` with no delete; `advanceBracket`'s `finalRound = Math.max(...all.map(m => m.round))` reads over *all* rows incl. ghosts). Note: a re-seed only orphans rows when the paid count crosses a power-of-two boundary (e.g. 8→4, not 8→6, since 6 still rounds up to bracket size 8) — the report's "two refunds" example is loose but the defect is real. Fix: `seedBracket` now reads the existing bracket first and, *after* writing the new plan, deletes any prior `BRACKET#` rows the new plan didn't overwrite (delete-after-write so a mid-op failure can't leave the bracket missing its real matches). With ghosts gone, `advanceBracket`'s `finalRound` is correct and the 3rd-place routing (`round === finalRound - 1`) fires again. Regression test in `tournaments.test.ts` (seed 8 → refund 4 → re-seed 4) proven **red→green** against dynalite: pre-fix the board kept 8 rows with a phantom round-3 Final and the semifinal loser was never routed to 3rd place; post-fix the board is exactly the 4 real rows (max round 2) and the loser routes correctly. Leagues have no bracket (schedules/standings), so no league equivalent.

### M10. League schedule regeneration orphans fixtures and wipes reported scores
- **Confidence:** CONFIRMED · `lib/data/leagues.ts:771-810` — put-only (no delete of existing `WEEK#` rows) and overwrites reused mids with `confirmStatus: "scheduled"`. Contrast `materializeStandings` (`:826`) which deletes stale rows.
- 6 entrants → 3 fixtures/week (several confirmed). One player refunded → organizer regenerates → 2 fixtures/week; every week's old `0002` fixture (confirmed) survives → `materializeStandings` counts it → standings double-count for the rest of the season.
- **✅ FIXED:** confirmed (`generateSchedule` was put-only with no delete; `materializeStandings` reads `schedule` and counts every `confirmStatus === "confirmed"` row incl. orphans). Key detail: the fixture `mid` is a *positional* index within a week (`${prefix}${pad(idx)}`), **not** matchup-stable — so a roster change re-points every reused mid at a different pairing and a shrinking roster drops trailing fixtures entirely; there is no meaningful way to preserve prior scores, and the documented intent is "regenerating overwrites (deterministic per roster)". Fix mirrors the sibling `materializeStandings` (which already clears stale rows) and the M9 bracket fix: `generateSchedule` now reads the existing schedule and, *after* writing the new fixtures, deletes any prior `WEEK#` rows the new plan didn't overwrite (delete-after-write so a mid-op failure can't leave the schedule missing real fixtures). Regression test in `leagues.test.ts` (6 paid → confirm all 3 week-1 fixtures → refund 2 → regenerate) proven **red→green** against dynalite: pre-fix week 1 kept 3 rows with the orphaned `did-0002` still `confirmed`; post-fix exactly 2 freshly-`scheduled` rows survive, zero confirmed ghosts, and standings recompute to a clean slate. (Tournaments have no weekly schedule — brackets, covered by M9.)

### M11. Ladder join race: no per-uid uniqueness → two rungs + two charges for one player
- **Confidence:** CONFIRMED · `lib/data/ladders.ts:474-496` — the duplicate guard is a non-atomic read; RUNG rows are keyed by *position*, not uid.
- Double-click "Register" → two pending rungs, two payable Checkout sessions. If both pay, the second confirm finds `.find(uid)` already `paid` → `alreadyPaid` → second charge captured, unfulfilled, unrecorded.
- **✅ FIXED:** confirmed (the duplicate guard is a non-atomic read; `appendRung`'s conditional `putNew` is on the *position* key, so two concurrent first-joins each grab a distinct position → two rungs, two Checkout sessions, a double charge — plus board corruption when `reorderBoard`'s by-uid Map collapses one uid at two positions). Fix uses the DynamoDB "unique-index-via-marker" pattern: added `ladderKeys.member(lid, uid)` (`MEMBER#<uid>` in the ladder partition) and, in the FIRST-join branch of `registerForLadder`, claim it via a conditional `putNew` *before* appending — exactly one concurrent writer wins, the loser 409s. The rejoin branch is untouched (a since-terminal rung is reused at its FIXED uid position, so concurrent rejoins converge on one row — no marker needed); the marker is rolled back on an `appendRung`/Checkout failure so a retry isn't permanently blocked. The `MEMBER#` row is inert to reads (`getRungs` filters `RUNG#`; `getLadder` keys off `entity`/`sk`), verified by the still-green pattern-22 one-query + rejoin tests. Regression test in `ladders.test.ts` (two concurrent `registerForLadder` for one uid) proven **red→green** against dynalite: pre-fix BOTH joins succeeded (two rungs); post-fix exactly one wins, the other is a 409, and one rung → one paid rung + one Payment. (Tournaments/leagues already reserve spots via a conditional counter, and their reg rows are uid-keyed, so no equivalent.)

### M12. Challenge status flips before the re-rank; a failed reorder loses the result permanently
- **Confidence:** CONFIRMED · `lib/data/ladders.ts:925-946` (`confirmChallengeResult`) and `:1005-1031` (`expireChallenges`) do the one-shot conditional `reported→confirmed` (or `open→expired`) *before* `applyOutcome`/`reorderBoard`, which throws after 6 version-guard retries (`:389`).
- On a busy ladder, if `reorderBoard` loses the race 6 times, the challenge is terminally `confirmed` but the win/movement was never applied — and re-calling the endpoint fails the `#st = :reported` condition (409). The upset is lost with no retry path.
- **✅ FIXED:** confirmed (`confirmChallengeResult` flips `reported→confirmed` then calls `applyOutcome`→`reorderBoard`, which throws a 409 after 6 version-guard retries; `expireChallenges` has the same shape for `open→expired`). Verified the key safety property: `reorderBoard` throws ONLY after all 6 attempts failed *without committing* (each attempt's `transactWrite` returns on success or is caught+retried), so an `applyOutcome` throw ⟺ the board is unchanged. Fix: wrap `applyOutcome` in try/catch and, on failure, roll the challenge status back to its pre-transition value (`confirmed→reported`, or `expired→open`) so the operation is RETRYABLE — the confirm re-throws the busy 409 (retry re-does it cleanly), the expire sweep rolls back to `open` and `continue`s (retried next sweep) instead of aborting. Considered but rejected folding the status transition into `reorderBoard`'s transaction (atomic, but a much larger refactor of a delicate concurrency file); rollback is localized and correct for the realistic failure (the only residual is the rollback update itself transiently failing — a different item, no version guard, so essentially never). Regression tests in `ladders.test.ts` for BOTH sites proven **red→green** against dynalite by forcing the re-rank to fail deterministically (a `vi.spyOn` rejects the `rungsVersion` bump — the transaction's first item — on every attempt): pre-fix the challenge is stuck `confirmed`/`expired` with the board never re-ranked; post-fix it's back to `reported`/`open`, the board is untouched, and a retry once contention clears confirms/expires AND applies the movement.

### M13. Aggregator ignores OUTING META `REMOVE` — geo `counts.games` never decrements
- **Confidence:** CONFIRMED (bites dev/CI today, not just future prod) · `lib/streams/aggregator.ts:225-236` (`onCreateGeoCount` early-returns on non-INSERT) vs `lib/data/outings.ts:586-616` (`deleteOuting` emits the META remove specifically to decrement `counts.games`).
- Create an outing → city/state/country `counts.games +1`; delete it → court `gamesCount` returns to 0 (via `onOutingRef` REMOVE) but the geo `counts.games` stays inflated forever, with no reconcile for it.
- **✅ FIXED:** confirmed (the dispatcher routed OUTING `META` — both INSERT and REMOVE — to `onCreateGeoCount`, which early-returns on any non-INSERT, silently dropping `deleteOuting`'s emitted META remove; the court `gamesCount` decrements only because the OUTINGREF remove goes through the separate `onOutingRef`). Fix mirrors the already-correct `onGroupMeta`: added a symmetric `onOutingMeta` (INSERT +1 / REMOVE -1 on geo `counts.games` via `bumpCityStateCountry`, reading `currentImage` so REMOVE attributes off the old image's `cityKey`) and rerouted the OUTING `META` dispatch to it. `onCreateGeoCount` stays INSERT-only for courts/players (whose organic-delete flows don't exist). Regression test in `outings.test.ts` (create an outing → both counters at 1 → `deleteOuting` → both back to 0) proven **red→green** against dynalite: pre-fix `counts.games` stayed stuck at 1 after the delete; post-fix it decrements to 0 alongside the court `gamesCount`.

### M14. Aggregator `counts.players` can never increment in the real signup flow
- **Confidence:** CONFIRMED · `lib/streams/aggregator.ts:416-419` handles `USER/PROFILE` INSERT only; but `lib/data/users.ts` never emits for profile writes (so dev/CI never reaches it), and `getOrCreateProfile` creates the profile **without** `homeCityKey` (it's set later by a MODIFY, which the INSERT-only handler skips).
- Every real user: sign in → profile created (INSERT, no city → skipped) → onboarding sets city (MODIFY → ignored) → city/state/country `counts.players` stays 0 in every environment. No re-attribution on a home-city change, either.
- **✅ FIXED:** confirmed on both facets. (A) **Handler**: replaced the INSERT-only `onCreateGeoCount(record, "players", "homeCityKey")` with a new `onProfile` that attributes on the homeCityKey EDGE — INSERT-with-city → +1; MODIFY where the city changes → -1 old (if any) + 1 new (if any), covering first-set-at-onboarding, a home-city move, and a clear; REMOVE → -1. So the count lands when the city becomes known and re-attributes when the user moves. This alone fixes PROD (real Streams fire on every write). (B) **Emit wiring**: `users.ts` `getOrCreateProfile` now `emitInsert`s the new profile and the `PUT /api/account/profile` route `emitModify`s (old→new) — no-ops in prod (real Streams own it), but they drive the inline aggregator in dev/CI so the metric works there too and is testable. `onCreateGeoCount` stays for COURT META (courts). Replay caveat is the same ADD-counter one the module already documents (healed by the reconcile sweep). Regression test in `streams.test.ts` drives `applyStreamRecord` through the real signup shape (city-less INSERT → set city → move city → remove) and is proven **red→green** against dynalite: pre-fix `counts.players` stayed 0 through the city-set MODIFY; post-fix it lands +1 in the home city, re-attributes on a move, and drops to 0 on delete.

### M15. All 14 metadata sitemaps are frozen at build time (no `revalidate`)
- **Confidence:** CONFIRMED · `app/sitemap.ts:19-29` exports neither `revalidate` nor a dynamic API, so per this Next version each `/sitemap/<id>.xml` is generated once at build. The `outings` segment enumerates only `today..today+6` computed at build (`lib/seo/sitemap.ts:393-427`); `news`/`tournaments`/`ladders`/`leagues`/`groups` change constantly.
- Deploy on Jul 2 → `/sitemap/outings.xml` advertises only Jul 2–8 forever; `/sitemap/news.xml` never lists a story published after the build. (The hand-built `/news-sitemap.xml` is correctly `force-dynamic`; the metadata sitemaps are not.)
- **✅ FIXED:** confirmed against this Next version's docs (`node_modules/next/dist/docs/…/metadata/sitemap.md` §"Good to know": `sitemap.js` is a Route Handler *cached at build by default unless it uses a Request-time API or dynamic config option*). Added `export const revalidate = 3600` to `app/sitemap.ts` — one config on the route covers all 14 generated segments + `static`, opting every `/sitemap/<id>.xml` into hourly ISR so `outings`'s `today..today+6` window rolls forward and news/tournaments/ladders/leagues/groups pick up new content. Chose ISR (not `force-dynamic`) to match the codebase's established pattern (`app/page.tsx` and the directory pages use `revalidate = 3600`) — a crawl sitemap doesn't need per-request freshness, and the CDN caches between regenerations (the Google-News-specific `/news-sitemap.xml` stays separately `force-dynamic` for its 48h window). Regression guard in `test/unit/seo.test.ts` (imports `@/app/sitemap`, asserts `revalidate` is a finite positive number) proven **red→green**: without the export it's `undefined` (build-frozen) and the test fails.

### M16. `MatchConfirmRow` / `AvailabilityToggle` rendered without a key — next week inherits the prior match's state
- **Confidence:** CONFIRMED · `components/leagues/ParticipantConsole.tsx:151-158,223` (no `key={thisWeek.mid}`); `MatchConfirmRow.tsx:33-38` seeds state from props at mount only.
- Player confirms Week 1 → refetch flips it `confirmed` → `thisWeek` becomes Week 2, but React reuses the same instance → the panel shows Week 2's header with Week 1's "Final — confirmed" status and scores; Week 2's inputs never appear until a full reload.
- **✅ FIXED:** confirmed both components (`MatchConfirmRow` seeds `status`/`reportedBy`/`scoreA`/`scoreB`/inputs via `useState(match.*)` at mount only; `AvailabilityToggle` seeds `value` from `status` at mount only; the parent rendered both without a `key`). Fix is the keyed-remount pattern (same as the H10 fix): `<MatchConfirmRow key={thisWeek.mid}>` and `<AvailabilityToggle key={upcomingWeek}>` in `ParticipantConsole.tsx`, so React unmounts+remounts the child when the fixture/week changes and its state re-seeds from the fresh props. Pure behavioral fix — no visual/layout change (a Chrome pass would render identically). Regression test `test/component/leagues/ParticipantConsole.test.tsx` (renders the console with mocked `useAuth`/`useLeague`; a `reported` Week 1 becomes `confirmed` so `thisWeek` advances to a `scheduled` Week 2) proven **red→green** under jsdom: pre-fix the matchup panel kept Week 1's "Confirm score/Dispute" reported UI and never showed Week 2's "Submit score" entry; post-fix the row remounts and shows Week 2's score entry.

### M17. Profile username check gets stuck at "Checking availability…" forever, blocking all saves
- **Confidence:** CONFIRMED · `components/account/ProfileEditor.tsx:136-149`; root cause `enabled: isSlug(username)` (`lib/api/profile.ts:78`).
- For any non-slug username (space, underscore, trailing hyphen, or cleared) the availability query is disabled → `availability` stays `undefined` → `usernameChecking` is permanently true → `canSave` permanently false, with no actionable error. No profile change (even unrelated fields) can be saved until a valid slug is restored.
- **✅ FIXED:** confirmed (the availability query is `enabled: isSlug(username)`, and the editor derived `usernameChecking` from just `!availability` — so a non-slug's never-resolving query masqueraded as a perpetual in-flight check with no error). Fix (client-only): `ProfileEditor` now validates the slug LOCALLY (`usernameSlugValid = isSlug(form.username)`) and gates every derived state on it — a non-slug is `usernameInvalid` IMMEDIATELY (renders the existing "Use lowercase letters, numbers, and hyphens." error), and `usernameChecking` is true only while the enabled, slug-valid query is genuinely in flight. So the user gets actionable feedback and can fix/restore the username instead of a silent dead-end. Regression test `test/component/account/ProfileEditor.test.tsx` (type a non-slug username, mocked hooks) proven **red→green** under jsdom: pre-fix no error appeared and it sat at "Checking availability…"; post-fix the error shows immediately and "Checking availability…" is absent. Full component suite green (200).

### M18. Round-robin "Time cap" setting is silently discarded on create
- **Confidence:** CONFIRMED · `app/round-robin/new/CreateClient.tsx:238` sends `scoring: { …, cap: null }`; `timeCapMin` (state at `:200`, select at `:450`) is only passed to the local preview, never into the created config; `RrConfig` has no time-cap field.
- Organizer picks "15 min" (and the preview even says "games may end in a tie at the 15-minute cap") → the created event, run console, and public board have no time cap. The chosen setting is lost.
- **✅ FIXED:** confirmed — and found a *second* strip point the report didn't name: even if the client sent the value, the server allowlist `parseScoring` (`app/api/round-robin/_util.ts`) rebuilds `scoring` from scratch carrying only `pointsToWin`/`winBy`/`cap`/`gamesPerMatch`, so `timeCapMin` would be dropped there too. Important distinction confirmed in the types: `ScoringConfig.cap` is a hard **points** ceiling ("at cap win-by is ignored"), NOT a time cap — so the report's "RrConfig has no time-cap field" is correct and wiring `timeCapMin` into `cap` would be wrong. Three coordinated changes: (1) added `timeCapMin?: number | null` to `ScoringConfig` (documented as wall-clock minutes, distinct from `cap`); (2) `CreateClient` now includes it in `config.scoring` (`...(timeCapMin ? { timeCapMin } : {})`) + added it to the config memo deps; (3) `parseScoring` now carries a positive integer or explicit null (following the `cap` allowlist pattern). It then round-trips through `createRrEvent` (which persists the full `config.scoring`) and the `RrEventMeta` projection. Scope note: the run console / public board don't display *any* scoring rules today (not even points-to-win), so surfacing the cap there would be a separate display feature; the in-scope defect (the setting being silently discarded) is what's fixed — the value is now persisted and already drives the builder's `SchedulePreview`. Regression test `test/unit/roundrobin-parse.test.ts` proven **red→green**: pre-fix `parseConfig(... timeCapMin: 15 ...)` returned `undefined`; post-fix it carries `15` (and treats absent/0 as none, explicit null as null). No visual change (data-flow fix).

### M19. Organizer refund failures are swallowed silently
- **Confidence:** CONFIRMED · `components/tournaments/OrganizerDashboard.tsx:91-97` — `refundMut.mutateAsync(...).catch(() => {})`, no error state anywhere.
- The Refund button flashes "Refunding…" then returns to "Refund" with the row still "paid" and zero feedback on failure → the organizer believes it worked (or double-clicks, compounding **H4**).
- **✅ FIXED:** confirmed (`doRefund` did `.mutateAsync(...).catch(() => {}).finally(...)` — no error state anywhere). Added a per-row `refundError` state and, on failure, populate it with the server message (or a clear fallback that states the refund was NOT applied); rendered as an inline `role="alert"` under the failing row's button, following the codebase's existing inline-error pattern (RegisterPanel / CreateTournamentWizard / the M16 fix) — no toast utility exists. Success feedback is unchanged (the invalidated refetch flips the row to `refunded`); the new attempt clears any prior error. Regression test `test/component/tournaments/OrganizerDashboard.test.tsx` (mocks `useTournament` + a rejecting `useRefundRegistration`, clicks Refund) proven **red→green** under jsdom: pre-fix the rejection was swallowed and no alert appeared; post-fix an alert shows the failure and the button unlocks (not stuck on "Refunding…"). Minor visual addition (danger-colored inline text matching the existing error style); behavior verified by the component test.

### M20. Follow / unfollow never invalidates the "Saved courts" list
- **Confidence:** CONFIRMED · `lib/api/community.ts:84-87` — `useFollowCourt` invalidates `["court", id, "following"]` (a key nothing queries) and never touches `accountListKeys.followedCourts` (`lib/api/account-lists.ts:56`). Global `staleTime` is 60s.
- User on /account/courts follows a court on its page, returns within a minute → the newly followed court is missing (or an unfollowed one still listed).
- **✅ FIXED:** confirmed on both halves — `useFollowCourt.onSuccess` invalidated only `communityKeys.following(courtId)` (`["court", id, "following"]`), and a repo-wide grep found **no query reads that key** (the court-page `FollowButton` keeps its own optimistic `following` state), so the invalidation was a no-op; and it never touched `accountListKeys.followedCourts` (`["me","followed-courts"]`), which the /account/courts `useFollowedCourts` query actually reads. Fix: also `invalidateQueries({ queryKey: accountListKeys.followedCourts })` in `onSuccess` (kept the harmless `following` invalidation for forward-compat). No import cycle (account-lists doesn't import community). Regression test `test/component/useFollowCourt.test.tsx` (`renderHook` + a spied `QueryClient`, `useAuthedFetch` stubbed to resolve) proven **red→green**: pre-fix the followed-courts key was never among the invalidations (the test times out waiting); post-fix it is invalidated on success.

### M21. Publish-retry creates duplicate draft events (league & tournament wizards)
- **Confidence:** CONFIRMED · `components/leagues/CreateLeagueWizard.tsx:127-182`, `components/tournaments/CreateTournamentWizard.tsx:141-163` — `publish()` does create-draft → N division POSTs → publish in one function; on any mid-flow failure the created `lid`/`tid` is discarded and re-clicking recreates from scratch.
- Division 2 of 3 fails server-side → organizer clicks Publish again → a second draft is created; the first orphaned draft (with partial divisions) lingers in "my leagues". Each retry adds another.
- **✅ FIXED:** confirmed in both wizards — `publish()` held the created `lid`/`tid` in a LOCAL var, so a mid-flow throw discarded it and the next click recreated from scratch. Fix: two `useRef`s per wizard (`createdIdRef`, `createdDivisionsRef`) persist progress across retries; a retry REUSES the existing draft (skips create when the ref is set) and RESUMES the division loop from the first not-yet-created one (so it also can't duplicate divisions that already landed). Refs reset naturally on unmount (success routes away). Known scope limit: editing draft-level fields *after* a partial failure reuses the already-created draft rather than re-creating with the edits — an edge beyond the reported duplicate-draft harm. Regression test `test/component/leagues/CreateLeagueWizard.publish.test.tsx` drives the full wizard (mocked create/authed/connect/CityPicker) so the first Publish fails on the division POST and a retry succeeds; proven **red→green**: pre-fix `useCreateLeague.mutateAsync` was called twice (a second orphan draft), post-fix exactly once. The tournament wizard received the identical fix (same refs pattern); covered against regression by the existing tournaments component suite.

### M22. League organizer "Send announcement" button does nothing
- **Confidence:** CONFIRMED · `components/leagues/LeagueOrganizerDashboard.tsx:352-357` — a textarea + styled "Send announcement" button with **no `onClick`**, no pending/disabled state, not labeled coming-soon.
- Organizer writes a weather-cancellation notice and taps Send → nothing happens, message silently lost, participants never notified.
- **✅ FIXED:** confirmed — and confirmed there is NO announcement backend to wire it to (no `/api/leagues/[id]/announce` route, no `useSendAnnouncement` hook). Wiring a real send (endpoint + organizer authz + participant fan-out via `createNotification` + client hook) is net-new feature work, not a bug fix. The proportionate fix makes the control HONEST rather than a silent no-op: disabled the textarea + button, added a "Coming soon" badge, and softened the copy to future tense ("…will be delivered… This isn't available yet") — matching the codebase's existing coming-soon pattern (RunConsole "Add player", CourtsGamesToggle). Regression test `test/component/leagues/LeagueOrganizerDashboard.test.tsx` (mocks `useLeague`, switches to the Messaging tab) proven **red→green**: pre-fix the button was enabled with no coming-soon label; post-fix the button + textarea are disabled and "Coming soon" is shown. If a real announcement feature is wanted, that's a separate task (backend + fan-out + hook + button wiring).

### M23. Deleting a group + a real streams consumer resurrects a ghost group with negative `memberCount`
- **Confidence:** PLAUSIBLE (latent — fires only once a prod streams Lambda exists) · `lib/data/groups.ts:869-903` (cascade delete; inline path deliberately skips member-remove emits) vs `lib/streams/aggregator.ts:278-300` (`onMember` unconditional `ADD memberCount` — and `ADD` on a missing item **creates** it).
- Owner deletes a 10-member group → 10 MEMBER REMOVE records → aggregator recreates `{GROUP#<id>/META, memberCount:-9}` with no name/slug/joinPolicy → `getGroupMeta` returns a truthy corrupt group → `joinGroup` proceeds instead of 404. Needs `ConditionExpression: attribute_exists(pk)` on the handler.
- **✅ FIXED:** confirmed the mechanism and upgraded from PLAUSIBLE → **real latent prod bug** (correctly benign in dev/CI: `deleteGroup` deliberately emits no member removes — comment "No member emits — META no longer exists" — so the inline aggregator never runs `onMember` for a deleted group; but prod's real Streams fire a REMOVE per cascade-deleted MEMBER# row automatically, and `updateItem` with `ADD` on a missing item CREATES it). Applied the report's suggested fix: `onMember`'s `updateItem` now carries `condition: "attribute_exists(pk)"` and swallows the `ConditionalCheckFailedException`, so a member change for a gone group is a no-op instead of resurrecting a ghost META. The live-META paths (INSERT active → +1, pending→active → +1, etc.) are unaffected — the existing `groups.test.ts` still passes. Regression test added to `streams.test.ts` (apply a MEMBER `REMOVE` for a fresh/non-existent group id, then assert `getGroupMeta` is still undefined) proven **red→green** against dynalite: pre-fix the unconditional ADD created a ghost `{memberCount:-1}` META; post-fix none exists. (Only `onMember` writes to a parent that gets genuinely deleted in a normal flow — courts/geo items aren't deleted — so this is the one handler needing the guard.)

### M24. Zero-price ("free") events create $0 Checkout sessions that real Stripe rejects
- **Confidence:** PLAUSIBLE (depends on Stripe's ~$0.50 minimum) · `lib/data/ladders.ts:166-171,479-510`; same for `addDivision` with `price: 0`.
- Register/join always creates a payment-mode session with `unit_amount: total`. A 0-amount session is rejected by real Stripe (the fake accepts it), so a free ladder/division 500s on every join in production — free events are unjoinable. Gate on `total > 0` and skip Checkout.
- **✅ FIXED:** confirmed the mechanism (`computeFees(money(0), absorb)` → `total = 0`; the FakeGateway accepts a $0 session so dev/CI is green, masking the prod-only Stripe rejection). Applied the report's fix across ALL THREE paid products (ladders, tournaments, leagues — the report cited ladders + "same for addDivision"): when `breakdown.total.amount <= 0`, SKIP `createCheckoutSession` and fulfil the join immediately by reusing each product's existing confirm function (`confirmLadderPayment` / `confirmRegistrationPayment` / `confirmLeaguePayment` — which flip the reg/rung to `paid`, write a $0 receipt, seed placement), returning the success URL as `checkoutUrl` so the client's `window.location` redirect is UNCHANGED. On a fulfil failure the pending reg + claimed spot are rolled back so the join stays retryable. Scope: the immediate-fulfil covers the common free case (spot claimed, non-partner); partner-pending and deferred-capture-waitlist free regs keep their status and just skip the $0 session (payment isn't due yet) — a note, since those are edges. Regression tests added to all three integration suites (register a player into a `price: 0` event with NO webhook/confirm call, assert the reg/rung is `paid` and the spot claimed) proven **red→green** against dynalite: pre-fix each stayed `pending` after a $0 session (which real Stripe would 500); post-fix each is `paid` immediately with no session (`checkoutSessionId === ""` for ladders). Full integration suite green (138).

### M25. "Add to calendar" `.ics` silently drops the recurrence (despite the route's own doc)
- **Confidence:** CONFIRMED · `lib/outings/rrule.ts:174-227` (`IcsEvent`/`toIcs` have no `rrule` field) and `app/outings/[id]/calendar.ics/route.ts:27-35` (never passes `outing.rrule`). The route comment claims "recurring series carry their RRULE through `toIcs`" — but no `RRULE:` line is ever emitted.
- A weekly outing → `/calendar.ics` → a VEVENT with only `DTSTART`/`DTEND` → the user's calendar shows a single one-off event and every later week is missing.
- **✅ FIXED:** confirmed all three: `IcsEvent`/`toIcs` had no `rrule` field, `toIcs` emitted no `RRULE:` line, and the route never passed `outing.rrule` (its own doc comment was aspirational). Fix: added `rrule?: string` to `IcsEvent`; `toIcs` now emits an `RRULE:<value>` line inside the VEVENT (after DTSTART/DTEND) when present; the route passes `outing.rrule`. The stored rrule is already a bare `FREQ=WEEKLY;…` value, and RRULE is a STRUCTURED property (its `;`/`,` are syntax) so it is NOT text-escaped — but it IS defensively normalized (strip a stray `RRULE:` prefix + any CR/LF) so a malformed value can't inject extra ICS lines. Regression tests in `test/unit/rrule.test.ts` proven **red→green**: a recurring event now emits `RRULE:FREQ=WEEKLY;INTERVAL=1` inside the VEVENT (pre-fix: no RRULE line), a one-off has none, and an injection attempt (`RRULE:FREQ=WEEKLY\r\nSUMMARY:evil`) collapses to a single clean RRULE line. Pure `toIcs` is fully unit-tested; the route change is a one-line pass-through (file-download route, no visual UI to verify in Chrome).

### M26. `nextOccurrences` goes empty for long-lived recurring outings (window anchored at DTSTART)
- **Confidence:** CONFIRMED (executed) · `lib/outings/rrule.ts:158-170` expands at most `max(52, n·4)` occurrences *from DTSTART*, then filters by `from`; consumed at `app/outings/[id]/page.tsx:174-176`.
- Executed: `FREQ=WEEKLY;BYDAY=MO,WE,FR` from `2026-01-01`, queried at `2026-07-02` → the expansion ends `2026-05-01` → `nextOccurrences` returns `[]`. A Mon/Wed/Fri game's "upcoming occurrences" silently vanishes after ~4 months (a plain weekly one after ~12 months). The scan should start near `from`, not DTSTART.
- **✅ FIXED:** confirmed (the `max(52, n·4)` expansion is anchored at DTSTART, so once `from` is past the ~52-occurrence window everything expanded is `< from` → `[]`). Fix: `nextOccurrences` now SIZES the expansion window to REACH `from` — `max = max(DEFAULT_MAX, (weeksToFrom + n + 2) · daysPerWeek)` where `weeksToFrom` is the interval-weeks between DTSTART and `from` — then still filters + slices. `expandRrule` keeps enforcing COUNT/UNTIL, so a genuinely-terminated series still correctly returns `[]` (only the unbounded/long-lived case is repaired). Added a `NEXT_OCC_MAX` (5200) runaway guard for an absurdly-far `from`. Kept the DTSTART anchor (RRULE phase/COUNT/UNTIL semantics require it) — only the window SIZE changed, matching the report's "start near `from`" intent without breaking recurrence math. Regression tests in `test/unit/rrule.test.ts` proven **red→green**: the report's exact MWF-from-Jan-queried-July case now returns 3 upcoming Mon/Wed/Fri occurrences (first = Fri Jul 3, pre-fix `[]`), and a plain-weekly-a-year-out case returns the next 2 (pre-fix `[]`); the two pre-existing near-`from` tests still pass (window clamps to `DEFAULT_MAX`).

### M27. Mixer hands one player almost all the byes (sits out 4 of 5 rounds)
- **Confidence:** CONFIRMED (executed) · `lib/roundrobin/engine/mixer.ts:45-69` — with an odd partnership count the leftover pair takes the bye, chosen by whatever the greedy opponent-picker leaves over; **bye history is never consulted**.
- Executed: 6-player popcorn mixer (5 rounds), seed 42 → byes `e0:1 e1:0 e2:2 e3:2 e4:4 e5:1` — one player plays once all event while another never sits (fair would be 2,2,2,2,1,1). Non-popcorn is worse (spread up to 6). Existing tests check partners/double-booking but never bye fairness.
- **✅ FIXED:** confirmed by executing the exact seed-42 case (spread 4). `seatTables` now, when the partnership count is odd, CHOOSES the sit-out pair by BYE FAIRNESS — minimizing `(max prior byes of the two players, then their sum)` with an rng tie-break — instead of benching whatever the greedy opponent-picker left over. A `byeCount` map (threaded through `generateMixer`) tracks total byes per player, including the circle-method SENTINEL bye (odd player counts) so the leftover bye balances against it. Partnering (circle method → zero repeat partners) and no-double-booking are untouched — the existing property tests still pass; determinism holds (rng still seeded). Only the bye SELECTION changed. Regression tests in `mixer.test.ts` proven **red→green**: the report's 6-player seed-42 popcorn case now has a bye spread ≤ 1 (was 4), and a property test over n=4–16 × 80 seeds asserts no player is benched more than `fairShare + 2` — measured worst post-fix `+2` (a per-round greedy's structural limit over a fixed partnership factorization) vs. pre-fix `+4` (a player benched 7 when fair was 3). Measured across 6500 samples: worst spread 6→4, and the mass shifts from spread 3–5 to 0–2.

### M28. Swiss pairing schedules an avoidable rematch (greedy, no backtracking)
- **Confidence:** CONFIRMED (executed) · `lib/roundrobin/engine/swiss.ts:66-76` takes the top player + first non-played opponent with no backtracking, violating its own "avoid rematches until unavoidable" contract.
- Executed with brute-force verification: seed 1, n=8, round 4 → greedy emitted `e4 vs e6` (already played round 3) though a rematch-free perfect matching existed; also n=8 round 5 and n=10 round 6. The existing test only covers 8 players / 3 rounds, where greedy happens to survive.
- **✅ FIXED:** confirmed by a brute-force oracle (a rematch is legitimate ONLY when no rematch-free perfect matching exists on the round's players). Replaced the later-round greedy with `pairAvoidingRematches` — small-n backtracking that MINIMIZES rematches: at each step it tries opponents non-rematch-first then nearest-record, keeps the first matching of the minimum rematch count (an all-`>= best.rematches` prune both bounds the search and preserves the nearest-preferring result), so it emits zero rematches whenever a rematch-free matching exists and the fewest otherwise. Round-1 fold, the bye rule (≤ 1/player, lowest unbyed), and determinism (no rng; pure fn of seed + scores) are untouched. Regression test in `swiss.test.ts` proven **red→green**: a property test over n=4–10 × rounds 3–6 × 120 seeds asserts (via the oracle) the engine NEVER schedules an avoidable rematch — pre-fix the greedy did; post-fix none. (The report's exact `e4 vs e6` depended on a scoring outcome I don't replicate with `sideAWins`, so its specific seed-1 case is kept only as a smoke check; the property test is the anchor.) The pre-existing "no rematch while avoidable (8p/3r)", bye-≤1, and determinism tests still pass; round-robin integration green.

### M29. Re-ingesting a smaller search index leaves stale chunks — deleted courts resurface in typeahead
- **Confidence:** CONFIRMED · `lib/search/index-store.ts:74-106` — `writeSearchIndex` writes chunks `0..N` but never deletes higher-numbered ones, and `loadChunks` concatenates *every* item under the `COURTS#`/`CITIES#` prefix (chunk size 800).
- First ingest 20,800 courts → chunks `0000..0025`; a cleaned re-ingest of 16,000 → writes `0000..0019`, leaving `0020..0025` (4,800 stale/deleted courts) in place → search suggestions return removed courts (and cross-chunk duplicates) until manually deleted. Same for cities.
- **✅ FIXED:** confirmed (`writeSearchIndex` was put-only by chunk key; `loadChunks` concatenates every chunk under the prefix). Fix: after writing the new chunks, `writeSearchIndex` now prunes any chunk whose index is ≥ the new chunk count via `deleteStaleChunks(pk, prefix, keep)` — a key-projected query (never loads the heavy `data` payloads) that deletes the leftovers, for BOTH `COURTS#` and `CITIES#`. Delete-after-write (not delete-then-write) so there's no empty-index window if it fails mid-op. Regression test `test/integration/search.test.ts` (write 2000 courts / 1200 cities → 3+2 chunks, then re-ingest 300 / 100 → 1+1 chunks) proven **red→green** against dynalite: pre-fix `loadSearchIndex` returned 1500 courts / 500 cities with removed rows (e.g. `c1999`) resurfacing; post-fix exactly 300 / 100 and the deleted rows are gone.

---

## LOW

- **L1. Unsubscribe token is forgeable (no signature).** `lib/notify.ts:104-121` mints `base64url("uid:email")` with no HMAC/expiry; `addUnsubscribe` (`lib/data/notifications.ts:184`) trusts the decoded email without checking it belongs to the account. An attacker who learns a uid (exposed in group member views) + email can permanently suppress a victim's emails. CONFIRMED. *(There's a `TODO(security)` on it.)*
  - **✅ FIXED:** the token is now HMAC-SHA256 signed — `makeUnsubToken` emits `base64url(uid:email).hmac`, and `parseUnsubToken` verifies the signature (constant-time `timingSafeEqual`), returning null on a forged or tampered token (the route already renders null as "invalid link"). Key from `UNSUBSCRIBE_SECRET` — **required in production** (an unset secret throws rather than silently signing with the public dev fallback → re-forgeable); a fixed fallback in dev/test. No expiry (an unsubscribe link must keep working for the email's life, RFC 8058). Signing also resolves the "trusts the decoded email" concern — we only ever sign addresses we send to. Regression tests in `notify.test.ts` proven **red→green**: a forged unsigned `base64(uid:email)` and a payload-swapped/stale-signature token now both return null (pre-fix both decoded to a usable `{uid,email}`); the legit round-trip still works.
- **L2. `calendar.ics` leaks private outing details with no visibility gate.** `app/outings/[id]/calendar.ics/route.ts:22-35` emits title/description/venue for **any** outing regardless of `visibility` (the RSVP route gates private outings; this doesn't). Mitigated by unguessable ULID ids. PLAUSIBLE.
  - **✅ FIXED:** scoped the gate to match the app's actual model. `Visibility` is `public | unlisted | private`; the RSVP route gates only `private` (unlisted is open-by-URL, like public), and the outing PAGE renders unlisted/private details by URL too (the token is an RSVP-time credential, not on the page URL — the page only reads `params`). So the fix targets **private** only: the `.ics` route now 404s a `private` outing (a portable calendar file can't carry the invite token, so it simply doesn't export one), and the page hides "Add to calendar" for private games. Public + unlisted are unchanged. Regression test in `outings.test.ts` calls the route's `GET` directly for all three visibilities — proven **red→green**: pre-fix a private outing returned 200 with its title/venue/description; post-fix 404, while public/unlisted still return 200 with `BEGIN:VCALENDAR`.
- **L3. Anonymous check-in daily cap is bypassable.** `app/api/courts/[courtId]/checkin/route.ts:108-140` — a **fresh** anon token skips the dedupe lookup, and `POST /api/anon-token` mints tokens freely, so looping token→checkin inflates a court's "today" count arbitrarily. PLAUSIBLE.
  - **✅ FIXED:** confirmed the loop (a fresh token = a new identity minted for free, so the per-token cap is Sybil-able). Anonymous identity is fundamentally Sybil-able without an IP/fingerprint (which the codebase doesn't surface), so the proportionate mitigation is a per-court, per-day CEILING on anonymous check-ins: the route now counts today's token-anon rows (`!c.uid`) and 429s once a court hits `MAX_ANON_CHECKINS_PER_COURT_PER_DAY` (100 — generous enough that real anonymous use never reaches it, bounding abuse to a fixed number instead of "arbitrary"). Account check-ins (uid-keyed, per-uid capped) are unaffected. Documented tradeoff: a hard ceiling *could* 429 a genuinely huge day at one court, but that's a far better failure than unbounded metric inflation of a vanity count. Regression test in `community.test.ts` (seed a court to the ceiling with raw anon rows, then a fresh-token POST) proven **red→green** against dynalite: pre-fix the fresh-token check-in returned 200 and bypassed the cap; post-fix it's 429.
- **L4. League registration trusts an arbitrary `teamId`.** `app/api/leagues/[id]/register/route.ts:29` → `lib/data/leagues.ts:579` stores `teamId` unvalidated; scheduling keys entrants by `teamId ?? uid` (`:761`), so a supplied teamId can collapse two registrations into one entrant and hijack fixtures. Bounded (needs a known teamId + paid entry). PLAUSIBLE.
  - **✅ FIXED:** `registerForLeague` now validates a supplied `opts.teamId` before claiming a spot: the team must EXIST (`getItem(leagueKeys.team(lid, teamId))`), belong to THIS division (`team.did === did` → else 400), and list the caller in `memberUids` (→ else 403). So a forged/made-up teamId, another division's team, or a team you don't belong to is rejected — you can't collapse your registration onto another entrant. Correction to the report's own coverage: the pre-existing `leagues.test.ts` "register variants" test registered with an arbitrary `team-${RUN}` and expected it to succeed — i.e. it encoded the vulnerable behavior; rewritten to `addLeagueTeam(...)` a real team first (like the H8 test-was-wrong case). New regression test proven **red→green**: a made-up teamId → 400, a real team the caller isn't in → 403, the caller's own team → succeeds (pre-fix all three "succeeded", storing the unvalidated teamId).
- **L5. `/login?next=` open redirect.** `components/auth/AuthPage.tsx:22` follows `next` via `router.replace` with no same-origin check → a phishing `?next=https://evil…` bounces the user off-site after a genuine sign-in. PLAUSIBLE.
  - **✅ FIXED:** added a pure, SSR-safe `safeNextPath()` (`lib/util/safe-redirect.ts`) that only accepts a genuine same-origin path — rejecting absolute URLs (`https://…`, `javascript:…` — no leading `/`), protocol-relative (`//host`), and backslash variants browsers normalize toward `//` (`/\host`) — defaulting to `/account`. `AuthPage` now computes `next = safeNextPath(params.get("next"))`, feeding both the signed-in bounce (`useEffect`) and the `onSuccess` redirect. Confirmed it's the ONLY `?next=` redirect consumer (the requireAuth flow uses `pendingIntent`, not the URL). Regression: a unit test covers the sanitizer's vectors, and a jsdom `<AuthPage>` test (mocked signed-in user + `?next=https://evil.com/pwn`) is proven **red→green** — pre-fix `router.replace` was called with the evil URL; post-fix with `/account`.
- **L6. `seedBracket` ignores DUPR despite its contract.** `lib/data/tournaments.ts:1108-1115` — the comment says "highest DUPR first" but the comparator sorts by registration time only; strongest players who registered last meet in round 1. CONFIRMED.
  - **✅ FIXED:** `seedBracket` now resolves each paid registrant's verified DUPR (`resolveDupr`, the same server-side RATING# source the entry gate uses) and sorts by DUPR descending, with registration time as the tie-break; unrated players sort last (`-Infinity`). So the strongest players get the top seeds and are spread across the bracket. Regression test in `tournaments.test.ts` registers 4 players in ASCENDING DUPR order (registration time = the opposite of seed order) and asserts seed 1 (round-1 M0 sideA) is the 4.5 player and seed 4 (M0 sideB) is the 3.0 — proven **red→green**: pre-fix seed 1 was the first-registered (3.0).
- **L7. `writePayment` receipts keyed by millisecond can overwrite.** `lib/data/payments.ts:91-110` — `PAYMENT#<ISO ms>` + plain `putItem`; two receipts for one uid in the same ms (concurrent fulfilments) collide and the second clobbers the first. PLAUSIBLE.
  - **✅ FIXED:** the fresh receipt sort key is now disambiguated — `ts = ${iso}#${ulid()}` — so the iso prefix preserves time ordering while the ulid guarantees uniqueness; a per-payer same-ms pair no longer collides. `ts` remains the per-uid key discriminator (refunds relocate by the stored `payment.ts` via `getPayment`/`refundPayment`), and an explicit `input.ts` is still honored verbatim, so nothing that locates a receipt broke — confirmed by the full payments + tournaments + leagues + ladders refund suites (56 tests) staying green. `createdAt` keeps the clean ISO. No consumer parses `ts` as a Date (verified). Regression test in `payments.test.ts` writes two receipts with the SAME `now` and asserts two distinct rows survive — proven **red→green**: pre-fix `getMyPayments` returned 1 (the second clobbered the first); post-fix 2.
- **L8. Correcting a score regresses a completed dynamic round-robin to "running".** `lib/data/roundrobin.ts:471-481` — dynamic events always get `nextStatus = "running"`, so re-scoring a finished event flips META back to `running` while `championId` stays set. CONFIRMED. ✅ FIXED — `recordScore` now keeps an already-`complete` dynamic event complete (`(!meta.dynamic && allScored) || (meta.dynamic && meta.status === "complete") ? "complete" : "running"`); a static-fully-scored event still completes here, and dynamic completion still happens via `advanceRound`. Regression test `test/integration/roundrobin.test.ts` "L8: correcting a score in a COMPLETE dynamic event keeps it complete" (proven red→green: pre-fix META flipped to `running`); full 9-test roundrobin suite green.
- **L9. `updateGroup` clobbers concurrent aggregator `memberCount` bumps.** `lib/data/groups.ts:757-798` re-puts the whole META with the `memberCount` it read moments earlier, racing the aggregator's atomic `ADD`; nothing heals it (reconcile has no callers). CONFIRMED. ✅ FIXED — replaced the full `putItem` with a targeted `updateItem` (SET the settings fields, REMOVE the ones being cleared, SET/REMOVE the GSI2 city-finder keys on a public/private flip) that never writes `memberCount`, so a concurrent aggregator `ADD memberCount` survives; `ReturnValues: ALL_NEW` gives the true persisted row back to the caller, and `attribute_exists(pk)` avoids resurrecting a group deleted mid-edit. Regression test `test/integration/groups.test.ts` "L9: a settings edit racing a join…" deterministically stages the lost-update window (spies the doc-client `send`, lands a real join right before updateGroup's META write) — proven red→green (pre-fix clobbered memberCount 2→1); full 13-test groups suite green (incl. the public→private re-keying test that the REMOVE now drives).
- **L10. Inline stream emits double-count on request races (dev/CI).** `lib/data/groups.ts:544-545,722-723`, `lib/data/reviews.ts:111-119` emit synthetic images from what the code path *thinks* happened, so two concurrent joins `emitInsert` twice (memberCount +2), two concurrent review edits `emitModify(3,5)` twice (ratingSum +4). CONFIRMED (needs a race). ✅ FIXED — the inline emit now derives its INSERT-vs-MODIFY image from the **atomic** prior state instead of a stale pre-read. Added `putItemReturningOld` (`PutCommand` with `ReturnValues: ALL_OLD`) to `lib/db/client.ts`; `joinGroup`/`acceptInvite`/`upsertReview` emit `INSERT` iff the prior image was empty else `MODIFY(prior→new)`. Under a race the winner emits the real delta and the loser a no-op MODIFY (`active→active` ⇒ 0, `5→5` ⇒ 0) — exactly what real DynamoDB Streams deliver, so the aggregator's delta math (`onMember`/`onReview`) stays correct. (Real prod was already safe because Streams compute true old/new images; this was a dev/CI-fidelity gap in the inline emulator.) Regression tests deterministically stage each race by spying the doc-client `send` and running the second op just before the first's row write commits: `test/integration/groups.test.ts` "L10: a double-submitted join…" (red: memberCount 3→green 2) and `test/integration/community.test.ts` "L10: two concurrent review edits…" (red: ratingSum 7 / avg 7 → green 5). Full 148-test integration suite green.
- **L11. `getCourtGames` reads the OUTINGREF partition oldest-first, unpaginated.** `lib/data/outings.ts:270-283` — once an active court's first 1 MB page is entirely past-dated, the upcoming-games grid shows zero games though future outings exist. PLAUSIBLE. ✅ FIXED — CONFIRMED on inspection (single `query`, `ascending: true`, then a JS `startTs >= now` filter: a full first page of PAST refs filters to `[]`). The read now bounds the sort key server-side and paginates: upcoming reads a `skBetween [OUTING#<now>, OUTING#￿]` range (past refs are never read — the read *starts* at the first upcoming game) via `queryAll` (follows `LastEvaluatedKey`, so no upcoming game is dropped); the unused `includePast` path keeps the full `begins_with` range. Regression test `test/integration/outings.test.ts` "L11: an upcoming game is found even when the first OUTINGREF page is all past-dated" seeds one past + one future public game and forces the OUTINGREF partition to return one row per page (a `send` spy that truncates the first page to the past row + a cursor) — reproducing the all-past first page without a million rows. Proven **red→green**: pre-fix the single non-paginated query saw only the past row and returned `[]`; post-fix the future game is present (and the past one absent). Full 14-test outings suite green.
- **L12. `markAllRead` only flips the first query page.** `lib/data/notifications.ts:140-159` — no `LastEvaluatedKey` loop, filter applied after the page → a user with >1 MB of notifications keeps unread rows after "mark all read". CONFIRMED. ✅ FIXED — swapped the single `query` for `queryAll` (the doc-blessed "mark all" sweep), so the read follows `LastEvaluatedKey` across every page before flipping; the `UNREAD_FILTER` is applied per-page but now every page is visited, so no unread row survives on a later page. Regression test `test/integration/notifications.test.ts` "L12: markAllRead flips unread rows BEYOND the first page (pagination)" seeds 3 unread rows and injects `Limit: 1` into the byOwner sweep query (via a `send` spy) so dynalite hands back a real `LastEvaluatedKey` per row — a >1 MB feed without the bulk. Proven **red→green**: pre-fix `markAllRead` flipped only 1 (returned 1, 2 stayed unread); post-fix it flips all 3 and `getUnreadCount` is 0. (Scoped to `markAllRead` per the report; `getUnreadCount`'s one-page count carries its own acknowledged TODO and is a display undercount, not a stuck-state bug.) Full notifications suite green.
- **L13. `createNews` republish leaves stale topic pointers.** `lib/data/content.ts:391-440` only *adds* `NEWSTOPIC#` pointers; re-publishing with a smaller topic set orphans the old ones, and `listNewsTopics` (`:235`) counts raw pointers → topic chips show counts that open an empty (filtered) feed. CONFIRMED. ✅ FIXED — `createNews` now RECONCILES the per-topic pointers instead of only adding: it reads the item's existing `TOPIC#` pointer rows (one bounded Query on the meta's own base partition, `pk=NEWS#<id>` / `begins_with(TOPIC#)`), writes the desired set, and DELETES any pointer whose topic is no longer wanted. The desired set is empty when the item is a draft, so an unpublish drops every pointer too. This kills both symptoms: `getNewsByTopic` no longer surfaces the item under a dropped topic (its orphan pointer resolved to a still-published parent and slipped past the status filter), and `listNewsTopics` no longer counts orphans into an empty (parent-filtered) feed. Regression test `test/integration/content.test.ts` "L13: re-publishing with a SMALLER topic set drops the orphaned topic pointer" publishes with `[T1,T2]`, re-publishes the same id with `[T1]`, and asserts T2's feed no longer contains it (T1's still does) — proven **red→green**: pre-fix the T2 pointer survived and the item stayed in T2's feed. Full 13-test content suite green.
- **L14. `getOrCreateProfile` race leaks an orphaned username reservation.** `lib/data/users.ts:234-260` — two concurrent first requests generate different usernames and both succeed; the earlier request's `USERNAME#` reservation is never released, so that handle is dead forever and the reservation↔profile invariant breaks. PLAUSIBLE. ✅ FIXED — the create transaction now guards the PROFILE row itself, not just the reservation: `putProfileWithUsername` adds `attribute_not_exists(pk)` to the profile `txPut` on the first-create path (`oldUsername === undefined`), so a concurrent first-access for the SAME uid can't atomically create a second profile + second reservation (an EDIT that renames — `oldUsername` defined — still overwrites, unchanged). `getOrCreateProfile` then treats a `UsernameTakenError` on create as "either a different uid took this name (retry with a suffix) OR a concurrent create of THIS uid already won" — a strongly-consistent re-read of the profile returns the winner's profile when present (so the loser never writes a second identity), else it retries. Net: exactly one profile + one matching reservation, and the aggregator INSERT is emitted once. Regression test `test/integration/profile.test.ts` "L14: two concurrent first-accesses for one uid don't orphan a username reservation" deterministically stages it (a `send` spy intercepts the outer call's `root` reservation claim and runs a second full `getOrCreateProfile` for the same uid first). Proven **red→green**: pre-fix the loser created a second profile at `root-<rand>`, orphaning the `root` reservation (`getUserByUsername(root)` → undefined); post-fix both calls resolve to the same `root` profile and `root` stays reserved for the uid. Full 9-test profile suite green.
- **L15. Court "local day" longitude approximation mislabels "checked in today".** `lib/directory/court-local-day.ts:18-25` uses `round(lng/15)` with no DST; for a ~1-hour window around local midnight the computed `yyyymmdd` is the wrong day → the ISR court page shows "0 checked in today" while people are on court. CONFIRMED (acknowledged coarseness, but produces wrong labels). ✅ FIXED — `courtLocalDay` now resolves the court's REAL IANA timezone (an explicit `court.tz` override if set, else a `tz-lookup` from `lat`/`lng`) and formats the calendar day in that zone via `Intl.DateTimeFormat.formatToParts`. Because a real zone (`America/New_York`, `Asia/Kolkata`, …) carries DST + political/half-hour offset rules, the day bucket flips at the court's TRUE midnight, so the check-in WRITE (`checkin` route + `createOuting`, both now pass the full court) and the court-page READ agree; the city-centroid pages pass `centroidLat` so they use the lookup too. The old `round(lng/15)` survives only as a FALLBACK when coordinates are absent or the lookup can't resolve the point. Added the `tz-lookup` dependency (chosen by the user over a latent forward-compatible fix or a court backfill), an ambient `types/tz-lookup.d.ts`, and an optional `tz?` on `CourtItem` (no migration — absent by default). Regression tests in `test/unit/checkins.test.ts` ("courtLocalDay uses the court's REAL IANA timezone (L15 — DST-correct)"): a US-East court at `2099-06-15T04:30Z` (00:30 EDT) → `20990615` — proven **red→green** (pre-fix the UTC-5 approximation gave `20990614`, the wrong day); plus an explicit-`tz` override case and a no-coordinates fallback case. Full suite: 618 pass; only the 2 pre-existing L23 geohash unit reds remain.
- **L16. `/near` uses a default (307) redirect with no cache guard.** `app/near/route.ts:13` — a per-visitor geo-IP redirect with no `Cache-Control: private`/`Vary`; a shared cache keyed on path could pin one visitor's nearest city for others. Mitigated on Vercel's edge. PLAUSIBLE. ✅ FIXED — the redirect now carries `Cache-Control: private, no-store`, so no shared/intermediary cache may store the per-visitor geo-IP result and replay one visitor's nearest city to everyone else (`no-store` forbids all caches; `private` is belt-and-braces). Defense-in-depth beyond Vercel's edge default. Also corrected the route docstring (it claimed "302s" but `NextResponse.redirect` defaults to 307). Regression test `test/unit/near-route.test.ts` calls `GET` with no geo headers (falls back to `/courts`) and asserts the redirect's `Cache-Control` contains `no-store` — proven **red→green** (pre-fix the header was absent → shared-cacheable).
- **L17. Weather forecast can freeze / show the wrong day inside the ISR outing page.** `app/outings/[id]/page.tsx:166` → `lib/weather.ts:66` bare `fetch` (no `cache`) inside a `revalidate = 600` page; combined with Open-Meteo `timezone=auto` vs `outing.tz` for `dayStr`, the `.date === dayStr` match can fail and fall back to the wrong day's weather. PLAUSIBLE. ✅ FIXED — both halves. (1) **Wrong day:** Open-Meteo buckets `daily.time[]` in the zone named by its `timezone` param; with `timezone=auto` (the LOCATION's zone) those days could differ from `dayStr` (computed in `outing.tz`), so `date === dayStr` missed and the page fell back to `forecastDays[0]` — TODAY's weather instead of the game's day. `getForecast` now takes an optional `{ tz }` and requests `&timezone=<encoded tz>`; the page passes `outing.tz`, the same zone `dayStr` uses, so the day match lands (falls back to `auto` only when the outing has no tz). (2) **Freeze/uncached:** confirmed against `node_modules/next/dist/docs` that THIS Next does **not** cache `fetch` by default (so the report's stock-Next "freeze" is here an uncached provider hit on every ISR regeneration); the forecast fetch now sets an explicit `next: { revalidate: 1800 }` for a weather-appropriate ISR data-cache window (still degrades to `null` on any failure). Regression test `test/unit/weather.test.ts` stubs `fetch` and asserts the request carries `timezone=America%2FChicago` (not `auto`) when a tz is passed, that `next.revalidate > 0`, and that a tz-less call still sends `timezone=auto` — proven **red→green** (pre-fix: always `auto`, no `next` option).
- **L18. Group without a home court can never schedule meet-ups.** `app/groups/[id]/manage/ManageGroupClient.tsx:211-214` tells the owner to "set a home court in Settings", but the Settings form (`:43-147`) has no home-court field though `UpdateGroupInput.homeCourtId` exists. Dead-ends group meet-ups. CONFIRMED. ✅ FIXED — the Settings form now has a **Home court** field. Extracted the create-group flow's `CourtSearch` typeahead (+ `cityFromCourtUrl`, `PickedCourt`) into a shared `components/groups/CourtSearch.tsx` (barrel-exported) and used it in both places (DRY, no behavior change to create). `SettingsForm` seeds the picker from the group's current home court (hydrated via the detail response's `courts` map) and, on save, sends `homeCourtId` when a court is picked; `useUpdateGroup` invalidates the group query so the meet-up scheduler un-gates immediately (`noCourt` flips false). The PATCH route + data layer already accept `homeCourtId`; the meet-up's own `cityKey` is derived server-side from the court, so scheduling works regardless of the group's stored city. Verified in Chrome (dev server): the reused `CourtSearch` renders and returns live typeahead results, console is clean (no errors/warnings), and the form is single-column full-width (mobile-safe). Regression tests `test/component/groups/ManageGroupClient.test.tsx` render the real console and assert (a) the Settings home-court combobox exists, (b) picking a court + Save sends `homeCourtId`, (c) the scheduler un-gates once a court is set — the field-existence + save-wiring tests proven **red→green** (pre-fix: no such field).
- **L19. `OutingWizard` allows an end time before the start time.** `components/outings/OutingWizard.tsx:447-450` builds `startTs`/`endTs` from independent selects with no ordering check → a negative-duration outing is submitted after the 5-step flow. CONFIRMED. ✅ FIXED — the "When" step now validates ordering: `timeOrderValid = endTime > startTime` (both are same-day zero-padded 24h `HH:MM`, so a string compare orders them), which gates `canAdvance(1)` (Next disabled until the end is strictly after the start) and drives an inline `role="alert"` message ("The end time needs to be after the start time."). `submit` also carries a defensive guard (bounces back to step 1 rather than POST a negative-duration game) even though the step gate makes it unreachable via the UI. Regression test `test/component/OutingWizard.timeorder.test.tsx` drives the REAL wizard (court prefilled via `?court=`) into the When step and, via `user-event` on the actual HeroUI Select, sets End to 8:00 AM before the 9:00 AM start — asserting the inline error appears and Next becomes disabled. Proven **red→green** (pre-fix: no error, Next stayed enabled). The live `/outings/new` route is auth-gated (redirects to `/login`), so it wasn't loaded in Chrome; the new error element is a verbatim reuse of the wizard's existing already-styled error `<p>` and the Next button uses its existing `disabled:` styling, both exercised by the component test.
- **L20. Signed-in members flash the "Sign in" gate while auth resolves.** `components/leagues/ParticipantConsole.tsx:76-89`, `components/ladders/ChallengeConsole.tsx:77-90` check `if (!user)` but ignore `useAuth().loading`; with real Firebase, `user` is null for the first few hundred ms → a member briefly sees "Sign in to view your team" (clickable). CONFIRMED. ✅ FIXED — both consoles now destructure `loading` from `useAuth()` and render the loading **skeleton while auth resolves**, *before* the `!user` gate: a signed-in member sees the skeleton (then their content) rather than a flash of the clickable "Sign in" panel; a genuinely signed-out visitor (auth settled, `loading:false`, `user:null`) still gets the gate. Reuses each console's existing skeleton markup (no new UI). Regression test `test/component/leagues/ParticipantConsole.authgate.test.tsx` renders the real console with a controllable `useAuth` mock — asserting `{loading:true,user:null}` shows NO "Sign in" gate (proven **red→green**: pre-fix it rendered), and `{loading:false,user:null}` shows the gate. The existing M16 ParticipantConsole test still passes; the ladders `ChallengeConsole` got the identical fix. (The flash is a transient during-auth-resolution state on auth-gated pages, so no Chrome session; the change only reorders existing gates and reuses the component's own already-styled skeleton.)
- **L21. `ChallengeRow` / `RosterManager` copy server state into local state and never resync.** `components/ladders/ChallengeRow.tsx:52-55`, `components/groups/RosterManager.tsx:25` — keyed by stable ids so the instance survives refetches, showing stale challenge status / roster after the server object changes. PLAUSIBLE. ✅ FIXED — CONFIRMED (both seed `useState` from props at mount only). Both now use React's "adjust state when a prop changes during render" pattern to **adopt the authoritative server value when it changes underneath the mounted instance**. `ChallengeRow` tracks a `${status}|${reportedBy}` server snapshot and, when it changes, resets local `status`/`reportedBy` (so a confirm/decline on another device, or the server-truth after an optimistic write, is reflected). `RosterManager` keeps a `prevMembers` ref and re-adopts `members` when the reference changes — React Query's structural sharing keeps that reference stable until the content actually changes, so it fires only on a real update and an in-flight optimistic overlay (prop unchanged) is never clobbered. Regression tests: `ChallengeRow.test.tsx` "L21: resyncs status…" rerenders the same instance from `reported`→`confirmed` and asserts the stale "Confirm result" button disappears; `RosterManager.test.tsx` "L21: adopts an updated members prop…" rerenders with a new pending request and asserts it appears. Both proven **red→green** (pre-fix the mount-time state persisted); existing ChallengeRow/RosterManager suites still pass.
- **L22. `getSearchIndex` in-flight dedupe ignores the country.** `lib/search/index-store.ts:121-123` — `if (inflight) return inflight;` returns whatever build is in flight regardless of the requested `country` (the cache check above *is* country-keyed). Concurrent `getSearchIndex("us")` + `getSearchIndex("ca")` → the second caller gets the US index. Harmless while US-only; a landmine for international expansion. CONFIRMED. ✅ FIXED — replaced the single `cache`/`inflight` slots with per-country maps (`cacheByCountry` / `inflightByCountry`). The in-flight dedupe is now keyed by country, so a concurrent `getSearchIndex("ca")` no longer receives the in-flight `us` build; the cache is also per-country, so alternating countries no longer thrash a shared slot (each keeps its own TTL entry). Same-country concurrency still dedupes to one build. Regression test `test/unit/search-index-store.test.ts` mocks `query` to return country-specific chunks and races `getSearchIndex("us")` + `getSearchIndex("ca")` in one `Promise.all` — asserting each resolves to its OWN index (proven **red→green**: pre-fix `ca` got `court-us`), plus a same-country dedupe check (two concurrent `gb` calls share one result).
- **L23. Two unit tests are red — they still assert the old 6-char GSI4 geohash partition.** `test/unit/keys.test.ts:37-40`, `test/unit/ingest.test.ts:82` expect `GEO#9yujxw` but the code correctly emits precision-4 `GEO#9yuj` (`GEO_PARTITION_PRECISION = 4`, deliberate; write/query sides agree). Not a runtime bug, but the red suite masks real regressions — fix the tests. CONFIRMED (executed: `vitest run test/unit` → 2 failed). ✅ FIXED — re-verified the code is correct and self-consistent: `courtKeys.geo` slices `GEO_PARTITION_PRECISION` (=4) and the query-side `coverSet` defaults to the SAME constant, so write + read agree on the 4-char partition. The bug was in the TESTS, which hardcoded the stale 6-char expectation. Updated `test/unit/keys.test.ts` to assert `GEO#9yuj` (and retitled "…4-char prefix") and `test/unit/ingest.test.ts` to `geohash.slice(0, 4)` — pinning the real precision-4 contract. Both now pass (`test/unit/keys.test.ts` + `test/unit/ingest.test.ts` → 18 passed); the whole unit suite is green, so the red no longer masks regressions.

---

## Checked and found NOT buggy (so you don't re-audit)

- Tournament/league/ladder create/publish/cancel/refund/divisions/schedule routes all re-check `organizerId === user.uid` (or gate in the data layer); groups PATCH/DELETE/invite/approve/remove all route through `requireManager`/`requireOwner`.
- The Stripe webhook verifies the signature against the **raw** body and is idempotent (`recordStripeEventOnce`); dev-auth is correctly blocked outside non-Production (`lib/auth/verify.ts:43`); `params`/`searchParams` are consistently awaited as Promises across every dynamic route.
- ICS *escaping*/UID/CRLF in `lib/outings/rrule.ts` is RFC-5545 correct (the bug is that recurrence is dropped — **M25**); `notFound()` is called on every missing-entity path and `generateMetadata` returns a noindex shell rather than throwing (no 500s); private/unlisted groups, drafts, and private profiles are correctly excluded from JSON-LD and metadata; the hand-built news sitemap escapes XML text.
- Round-robin core is sound under execution: `roundRobin` (n=2–13, ±twice — every pair exactly once/twice, no double-booking, rotating byes), `movement` (exhaustive seatings, FIFO waiting pool), league `buildWeeklySchedule`/standings (pair coverage, bye fairness, head-to-head), ladder `applyResult`/`canChallenge`/`dueDateFrom`, `money` (integer cents, fee modes, zero-decimal currencies), `geohash.coverSet` (360° perimeter sweep, 0 misses), `slugify` (idempotent on unicode/apostrophe/CJK), weather parsing/units, court-filters, and suggest ranking all passed.
