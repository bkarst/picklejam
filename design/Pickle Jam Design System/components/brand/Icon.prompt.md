Outlined line icon matching the brand's 2px rounded stroke style (Lucide path set).

```jsx
<Icon name="trophy" size={22} />
<Button iconRight={<Icon name="arrow-right" size={18} />}>Read more</Button>
```

Available: search, menu, bell, arrow-right, chevron-right, play, trophy, users, calendar, trending-up, map-pin, mic, camera, message-circle, sparkles, newspaper, heart, share, clock, check, plus, x, star. Inherits `currentColor` — set color via the parent's `color`.
