The Pickle Jam brand mark. Renders the shipped SVG assets.

```jsx
<Logo variant="full" height={56} assetBase="../../" />
```

Variants: `full` (net + PICKLE JAM wordmark), `badge` (lime PJ circle), `ball` (pickleball), `net`. Set `assetBase` to the relative path that reaches the `/assets` folder from your page.
