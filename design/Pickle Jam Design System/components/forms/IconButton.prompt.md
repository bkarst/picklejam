Icon-only button for toolbars and headers (menu, search, notifications).

```jsx
<IconButton variant="ink" label="Search"><Icon name="search" /></IconButton>
```

Variants: `ink` `lime` `pink` `outline` `ghost`. Sizes `sm` `md` `lg`. Set `round` for a circular button. Always pass `label` for accessibility.
