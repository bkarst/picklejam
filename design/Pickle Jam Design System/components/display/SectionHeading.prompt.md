Section header: pink eyebrow overline + heavy uppercase title, optional trailing action.

```jsx
<SectionHeading overline="Latest" title="Top Stories"
  action={<Button variant="outline" size="sm">See all</Button>} />
```

Sizes `sm` `md` `lg`.
