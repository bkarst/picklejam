Big Archivo-Black number with an uppercase caption — scores, standings, counts.

```jsx
<Stat value="11–9" label="Final score" accent="pink" />
```

`accent`: `lime` (adds the green drop shadow), `pink`, `ink`.
