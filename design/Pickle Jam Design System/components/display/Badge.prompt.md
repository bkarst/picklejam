Loud uppercase status pill for labels like TOP STORY, LIVE, NEW.

```jsx
<Badge variant="pink">Top Story</Badge>
<Badge variant="lime" dot>Live</Badge>
```

Variants: `pink` `lime` `ink` `cream`. Sizes `sm` `md`. `dot` adds a leading status dot.
