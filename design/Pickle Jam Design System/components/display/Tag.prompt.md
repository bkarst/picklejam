Pill-shaped category or filter chip used in nav bars and filter rows.

```jsx
<Tag active iconLeft={<Icon name="newspaper" size={16} />}>News</Tag>
<Tag>Tournaments</Tag>
```

`active` fills it dark green. Pass `onClick` to make it interactive.
