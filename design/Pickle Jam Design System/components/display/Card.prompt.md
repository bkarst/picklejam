Rounded content surface / container.

```jsx
<Card variant="sticker" pad={24}>…</Card>
```

Variants: `flat` (soft shadow, default), `sticker` (dark-green outline + hard offset shadow — the signature look), `ink` (dark fill for feature blocks).
